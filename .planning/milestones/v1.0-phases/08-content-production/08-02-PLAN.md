---
phase: 08-content-production
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - public/assets/data/rooms/forest_clearing.json
  - public/assets/data/rooms/cave_entrance.json
  - public/assets/data/rooms/village_path.json
  - public/assets/data/rooms/cave_depths.json
  - public/assets/data/rooms/crystal_grotto.json
  - public/assets/data/items.json
  - public/assets/data/npcs/npcs.json
  - public/assets/data/ink-source/old_man.ink
  - public/assets/data/ink-source/narrator_history.ink
  - src/game/scenes/Preloader.ts
  - src/game/state/GameStateTypes.ts
autonomous: true
requirements: [NARR-01, NARR-02]

must_haves:
  truths:
    - "Player can play through the demo chapter (30-60 minutes) from start to a satisfying act gate"
    - "All three puzzle types (inventory, environmental, conversation) appear in the demo"
    - "At least 5 unique death scenarios exist across demo rooms with sardonic narrator commentary"
    - "NPCs provide puzzle-critical information through branching ink dialogue"
    - "No unwinnable states exist -- every puzzle remains solvable regardless of prior actions"
    - "Existing 3 rooms are updated and 2-5 new rooms are added for demo completeness"
  artifacts:
    - path: "public/assets/data/rooms/forest_clearing.json"
      provides: "Updated starting room with production-quality content"
      contains: "forest_clearing"
    - path: "public/assets/data/rooms/cave_depths.json"
      provides: "New room behind the locked door"
      contains: "cave_depths"
    - path: "public/assets/data/items.json"
      provides: "All demo chapter items registered"
      contains: "items"
    - path: "public/assets/data/npcs/npcs.json"
      provides: "All demo chapter NPCs registered"
      contains: "npcs"
    - path: "src/game/scenes/Preloader.ts"
      provides: "All demo room JSONs, dialogue, and audio registered for loading"
      contains: "load.json"
  key_links:
    - from: "public/assets/data/rooms/*.json"
      to: "public/assets/data/items.json"
      via: "Item IDs in room items[] match items.json entries"
      pattern: "\"id\":\\s*\""
    - from: "public/assets/data/rooms/*.json"
      to: "public/assets/data/npcs/npcs.json"
      via: "NPC IDs in room npcs[] match npcs.json entries"
      pattern: "\"id\":\\s*\""
    - from: "src/game/scenes/Preloader.ts"
      to: "public/assets/data/rooms/*.json"
      via: "Preloader loads all room JSONs by room-{id} key"
      pattern: "load\\.json\\('room-"
    - from: "public/assets/data/rooms/*.json"
      to: "public/assets/data/rooms/*.json"
      via: "Exit targetRoom IDs reference valid room files"
      pattern: "\"targetRoom\""
---

<objective>
Author the complete Demo Chapter (Act 1a): update the 3 existing rooms to production quality and add 2-5 new rooms, creating a complete 30-60 minute playable experience with inventory puzzles, conversation puzzles, death scenarios, and NPC dialogue.

Purpose: The demo chapter validates the game's pacing, tone, puzzle difficulty, and overall experience before committing to the full 5-hour game. This is the most critical content milestone.

Output: 5-8 production-quality room JSON files, updated items.json, updated npcs.json, updated/new ink dialogue scripts, and Preloader registration for all new assets.
</objective>

<execution_context>
@/Users/abel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-content-production/08-RESEARCH.md
@.planning/phases/08-content-production/08-01-SUMMARY.md

# Story bible and design docs (authored in Plan 01)
@.planning/phases/08-content-production/story-bible.md
@.planning/phases/08-content-production/puzzle-dependency-graph.md
@.planning/phases/08-content-production/room-map.md

# Existing content to update
@public/assets/data/rooms/forest_clearing.json
@public/assets/data/rooms/cave_entrance.json
@public/assets/data/rooms/village_path.json
@public/assets/data/items.json
@public/assets/data/npcs/npcs.json

# Engine types for reference
@src/game/types/RoomData.ts
@src/game/types/PuzzleData.ts
@src/game/types/ItemData.ts
@src/game/scenes/Preloader.ts
@src/game/state/GameStateTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author demo chapter room JSONs, items, and puzzles</name>
  <files>
    public/assets/data/rooms/forest_clearing.json
    public/assets/data/rooms/cave_entrance.json
    public/assets/data/rooms/village_path.json
    public/assets/data/rooms/cave_depths.json
    public/assets/data/rooms/crystal_grotto.json
    public/assets/data/items.json
  </files>
  <action>
Using the story bible and puzzle dependency graph from Plan 01 as the authoritative reference, author all demo chapter room JSON files and item definitions.

**For the 3 existing rooms (forest_clearing, cave_entrance, village_path):**
- Update to match the story bible's content plan for these rooms
- Add any missing hotspots, items, puzzles, death triggers, or dynamic descriptions specified in the story bible
- Ensure exits connect to new rooms as defined in the room map
- Maintain the existing sardonic narrator voice and dark comedy tone
- Keep existing content that still fits the story; revise what doesn't

**For new rooms (cave_depths and any others defined in story bible for Act 1a):**
- Create complete room JSON files following the exact RoomData interface format
- Each room must have: description, walkableArea polygon, exits, 2-4 hotspots with all verb responses, playerSpawn
- Add items, puzzles, deathTriggers, deaths, dynamicDescriptions, audio as specified in story bible
- All hotspot responses must be in-character sardonic narrator voice (2-3 sentences, specific and funny, never generic)
- Death narrator texts must be unique, dark comedy, reference the specific player action

**For items.json:**
- Add all new items specified in the story bible for Act 1a
- Keep existing 5 items, add new ones for demo chapter puzzles
- Each item needs: id, name, description (sardonic), combinable boolean
- Ensure every item that appears in a room's items[] array has a matching items.json entry

**Background layers:** Use the existing shared parallax layers (bg-sky, bg-mountains, bg-trees, bg-ground) with worldWidth 1920 for all rooms. Production art will come later; placeholder backgrounds work fine.

**Puzzle implementation rules:**
- Follow puzzle dependency graph exactly -- conditions and actions must match the graph's prerequisites and outputs
- Items consumed only on successful puzzle completion
- Death triggers checked AFTER puzzles (engine evaluation order)
- Use "once": true for one-time puzzles
- Combine puzzles must check both orderings (A+B and B+A)
- Environmental puzzles use flag conditions

**Audio:** Assign music and ambient tracks from existing placeholder audio. Reuse music-forest, music-cave, music-village, and ambient tracks. New rooms can share area-appropriate music (cave rooms get music-cave, etc).

**Walkable areas:** Use simple convex rectangles matching the existing room patterns: approximately x:50-910, y:380-520 for standard rooms. Adjust per room as needed.
  </action>
  <verify>
1. All room JSON files parse as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('public/assets/data/rooms/cave_depths.json'))"` (repeat for each new room)
2. Every item referenced in room items[] exists in items.json
3. Every exit targetRoom references a room that has a JSON file
4. Every puzzle trigger.subject references a valid hotspot, item, or exit ID within its room
5. Every deathTrigger references a deathId that exists in the same room's deaths object
6. Run `npx vitest run` to verify no type errors in existing tests
  </verify>
  <done>5-8 room JSON files exist with complete content (hotspots, items, puzzles, deaths, dynamic descriptions, audio). All item IDs are registered in items.json. All exits connect to valid rooms. The demo chapter puzzle chain is fully authored matching the dependency graph.</done>
</task>

<task type="auto">
  <name>Task 2: Author demo NPC dialogue, update narrator history, and register all assets in Preloader</name>
  <files>
    public/assets/data/npcs/npcs.json
    public/assets/data/ink-source/old_man.ink
    public/assets/data/ink-source/narrator_history.ink
    src/game/scenes/Preloader.ts
    src/game/state/GameStateTypes.ts
  </files>
  <action>
**NPC Registry (npcs.json):**
- Update with all demo chapter NPCs from the story bible
- Keep existing old_man entry, add 1-2 new NPCs for the demo
- Each NPC needs: id, name, personality, knowledge, dialogueKey, defaultGreeting
- dialogueKey must follow the pattern: "dialogue-{npc_id}"

**Ink Dialogue Scripts:**
- Update old_man.ink to match the story bible's expanded dialogue tree for the demo chapter
- Maintain existing EXTERNAL function declarations and the established conversation pattern
- Add conversation branches for new demo chapter puzzles/items/knowledge
- Create new .ink files for each new NPC (1-2 files)
- Each ink script must: declare all 7 EXTERNAL functions, have a greeting knot, use #speaker and #emotion tags, support branching based on game state flags, provide puzzle-critical information

**Narrator History (narrator_history.ink):**
- Expand to cover all demo chapter rooms, flags, and events
- Add commentary for new rooms visited, new flags set, death count milestones
- Maintain the existing pattern of conditional commentary blocks

**Compile ink scripts:**
- Run `node scripts/compile-ink.mjs` to compile all ink source to JSON
- Verify compiled files exist in public/assets/data/dialogue/

**Preloader Registration (Preloader.ts):**
- Register ALL new room JSON files: `this.load.json('room-{id}', 'assets/data/rooms/{id}.json')`
- Register ALL new NPC dialogue files: `this.load.json('dialogue-{npc_id}', 'assets/data/dialogue/{npc_id}.ink.json')`
- Do NOT add new audio or image assets (using existing placeholders)
- Keep all existing Preloader entries unchanged

**Starting Room (GameStateTypes.ts):**
- Verify the starting room is still forest_clearing (or update if story bible specifies differently)
- The default state's currentRoom should be the story's actual starting room

**Important:** After writing ink scripts, compile them by running the compile-ink.mjs script. If the script fails, check ink syntax -- common issues include: missing knot headers, unclosed conditionals, referencing undefined knots.
  </action>
  <verify>
1. `node scripts/compile-ink.mjs` completes without errors
2. Compiled .ink.json files exist for every NPC in npcs.json
3. Every NPC's dialogueKey in npcs.json matches a Preloader.ts load.json call
4. Every new room JSON file has a matching Preloader.ts load.json call
5. `npx vite build` completes without TypeScript errors
6. `npx vitest run` passes all existing tests
  </verify>
  <done>All demo chapter NPCs are registered with complete ink dialogue scripts. Narrator history covers demo chapter events. Preloader loads all demo chapter assets. The game compiles and existing tests pass. The demo chapter is fully wired and playable from forest_clearing through the Act 1a gate.</done>
</task>

</tasks>

<verification>
1. All room JSON files are valid JSON and follow the RoomData interface
2. The demo chapter forms a connected graph (every room reachable from forest_clearing)
3. All item IDs, NPC IDs, and flag names are consistent across rooms, items.json, npcs.json, and ink scripts
4. Ink scripts compile successfully
5. Preloader registers every new asset
6. `npx vite build` succeeds
7. `npx vitest run` passes
8. Demo chapter has at least 5 death scenarios, at least 10 puzzles, and at least 2 NPCs
</verification>

<success_criteria>
A complete, playable demo chapter exists: 5-8 interconnected rooms with inventory puzzles, conversation puzzles, environmental puzzles, death scenarios with sardonic narrator commentary, NPC dialogue with branching trees, and proper Preloader registration. The game builds and all tests pass. A player could play from forest_clearing through the Act 1a gate in 30-60 minutes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-production/08-02-SUMMARY.md`
</output>
