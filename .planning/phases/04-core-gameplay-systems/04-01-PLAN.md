---
phase: 04-core-gameplay-systems
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/game/state/GameState.ts
  - src/game/state/GameStateTypes.ts
  - src/game/state/SaveManager.ts
  - src/game/types/ItemData.ts
  - src/game/types/PuzzleData.ts
  - src/game/systems/PuzzleEngine.ts
  - src/game/__tests__/GameState.test.ts
  - src/game/__tests__/PuzzleEngine.test.ts
  - src/game/__tests__/SaveManager.test.ts
autonomous: true
requirements:
  - PUZ-04
  - PUZ-05
  - PUZ-07
  - DEATH-03
  - DEATH-04
  - UI-01
  - UI-02

must_haves:
  truths:
    - "GameState stores inventory, flags, visited rooms, removed items, and current room as a serializable bag of state"
    - "PuzzleEngine evaluates condition arrays against GameState and executes action arrays when all conditions pass"
    - "SaveManager serializes GameState to localStorage for auto-save and 5 manual save slots"
    - "Puzzle conditions and actions are data-driven types, not hardcoded logic"
  artifacts:
    - path: "src/game/state/GameState.ts"
      provides: "Singleton game state with inventory, flags, rooms, serialization"
      exports: ["GameState"]
    - path: "src/game/state/GameStateTypes.ts"
      provides: "GameStateData interface, default state factory"
      exports: ["GameStateData"]
    - path: "src/game/state/SaveManager.ts"
      provides: "Auto-save and manual save/load via localStorage"
      exports: ["SaveManager", "SaveSlotInfo"]
    - path: "src/game/types/PuzzleData.ts"
      provides: "PuzzleCondition, PuzzleAction, PuzzleDefinition types"
      exports: ["PuzzleCondition", "PuzzleAction", "PuzzleDefinition"]
    - path: "src/game/types/ItemData.ts"
      provides: "ItemDefinition type for master item registry"
      exports: ["ItemDefinition"]
    - path: "src/game/systems/PuzzleEngine.ts"
      provides: "Condition evaluator and action executor"
      exports: ["PuzzleEngine"]
  key_links:
    - from: "src/game/systems/PuzzleEngine.ts"
      to: "src/game/state/GameState.ts"
      via: "GameState.getInstance()"
      pattern: "GameState\\.getInstance"
    - from: "src/game/state/SaveManager.ts"
      to: "src/game/state/GameState.ts"
      via: "serialize/deserialize"
      pattern: "state\\.serialize|state\\.deserialize"
    - from: "src/game/systems/PuzzleEngine.ts"
      to: "src/game/types/PuzzleData.ts"
      via: "PuzzleCondition/PuzzleAction type imports"
      pattern: "import.*PuzzleData"
---

<objective>
Build the foundational data layer for Phase 4: GameState singleton, typed puzzle condition/action system, PuzzleEngine evaluator, and SaveManager for localStorage persistence. All systems are pure TypeScript logic with no Phaser or DOM dependencies, making them ideal TDD candidates.

Purpose: Every other Phase 4 system (inventory, death, narrator, menus) reads from and writes to GameState. The PuzzleEngine evaluates data-driven conditions/actions from JSON. SaveManager handles persistence. These must be rock-solid before integration.

Output: 6 source files + 3 test files covering GameState, PuzzleEngine, and SaveManager with full RED-GREEN-REFACTOR cycles.
</objective>

<execution_context>
@/Users/abel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-gameplay-systems/04-RESEARCH.md
@src/game/types/RoomData.ts
@src/game/EventBus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create type definitions and GameState with TDD</name>
  <files>
    src/game/state/GameStateTypes.ts
    src/game/types/ItemData.ts
    src/game/types/PuzzleData.ts
    src/game/state/GameState.ts
    src/game/__tests__/GameState.test.ts
  </files>
  <action>
RED phase:
Create `src/game/__tests__/GameState.test.ts` with tests covering:
- `GameState.getInstance()` returns same instance (singleton)
- `reset()` returns state to defaults (currentRoom='forest_clearing', empty inventory, empty flags)
- `addItem('key')` then `hasItem('key')` returns true
- `removeItem('key')` then `hasItem('key')` returns false
- `addItem` is idempotent (adding same item twice doesn't duplicate)
- `setFlag('door-open', true)` then `isFlagSet('door-open')` returns true
- `setFlag('door-open', false)` then `isFlagSet('door-open')` returns false
- `markRoomVisited('cave')` adds to visitedRooms, calling again doesn't duplicate
- `markRoomItemRemoved('forest', 'key')` then `isRoomItemRemoved('forest', 'key')` returns true
- `serialize()` returns valid JSON string, `deserialize()` restores state
- `getData()` returns readonly snapshot

Run tests -- they MUST fail (classes don't exist yet).

GREEN phase:
Create type definitions first:

`src/game/state/GameStateTypes.ts`:
```typescript
export interface GameStateData {
    currentRoom: string;
    inventory: string[];
    flags: Record<string, boolean | string>;
    visitedRooms: string[];
    removedItems: Record<string, string[]>;
    playTimeMs: number;
    deathCount: number;
}
```

`src/game/types/ItemData.ts`:
```typescript
export interface ItemDefinition {
    id: string;
    name: string;
    description: string;
    /** Whether item can be combined with others */
    combinable?: boolean;
}
```

`src/game/types/PuzzleData.ts`:
Define PuzzleCondition (discriminated union: 'has-item', 'not-has-item', 'flag-set', 'flag-not-set', 'in-room', 'item-not-taken'), PuzzleAction (discriminated union: 'add-item', 'remove-item', 'set-flag', 'remove-flag', 'show-message', 'narrator-say', 'trigger-death', 'remove-hotspot', 'add-hotspot', 'open-exit'), and PuzzleDefinition (id, trigger {verb, subject?, target?}, conditions[], actions[], once?). Follow the exact types from the research document's Pattern 2 section.

`src/game/state/GameState.ts`:
Singleton class with private constructor, static getInstance(), private data: GameStateData initialized via getDefaultState() (currentRoom: 'forest_clearing'). Methods: hasItem, addItem, removeItem, getFlag, setFlag, isFlagSet, markRoomVisited, isRoomItemRemoved, markRoomItemRemoved, serialize (JSON.stringify), deserialize (JSON.parse), reset, getData (returns Readonly).

Run tests -- they MUST pass.

REFACTOR: Clean up any redundancy. Ensure all exports are named (no default exports).
  </action>
  <verify>Run `npx vitest run src/game/__tests__/GameState.test.ts` -- all tests pass.</verify>
  <done>GameState singleton manages inventory, flags, rooms with full serialization. All 11+ test cases pass. Type definitions for items and puzzles are exported and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Create PuzzleEngine and SaveManager with TDD</name>
  <files>
    src/game/systems/PuzzleEngine.ts
    src/game/state/SaveManager.ts
    src/game/__tests__/PuzzleEngine.test.ts
    src/game/__tests__/SaveManager.test.ts
  </files>
  <action>
RED phase -- PuzzleEngine tests (`src/game/__tests__/PuzzleEngine.test.ts`):
- `evaluateConditions([{ type: 'has-item', item: 'key' }])` returns false when inventory empty
- `evaluateConditions([{ type: 'has-item', item: 'key' }])` returns true after adding 'key' to GameState
- `evaluateConditions` with multiple conditions returns true only when ALL pass
- `evaluateConditions` with 'flag-set' checks GameState flags
- `evaluateConditions` with 'not-has-item' returns true when item not in inventory
- `evaluateConditions` with 'item-not-taken' checks removedItems in GameState
- `executeActions([{ type: 'add-item', item: 'sword' }])` adds 'sword' to GameState.inventory
- `executeActions([{ type: 'set-flag', flag: 'door-open' }])` sets flag in GameState
- `executeActions` with 'narrator-say' returns the text string
- `executeActions` with 'trigger-death' emits EventBus event (mock EventBus or spy on emit)
- `tryPuzzle('take', 'key', null, puzzles)` matches trigger and executes when conditions pass
- `tryPuzzle` returns null when no puzzle matches
- `tryPuzzle` with `once: true` marks puzzle solved via flag, second call returns null

Reset GameState in beforeEach to ensure test isolation.

RED phase -- SaveManager tests (`src/game/__tests__/SaveManager.test.ts`):
- `autoSave` writes to localStorage key 'kqgame-autosave'
- `loadAutoSave` restores GameState from localStorage
- `hasAutoSave` returns false when no save exists, true after autoSave
- `saveToSlot(1)` writes to 'kqgame-save-1'
- `loadFromSlot(1)` restores state
- `getSlotInfos()` returns array of 5 entries (null for empty slots, SaveSlotInfo for filled)
- `loadFromSlot` returns false for empty slot

Use a mock or jsdom's localStorage (Vitest provides this by default with --environment jsdom). Add `// @vitest-environment jsdom` comment at top of SaveManager test if needed, or configure vitest to use jsdom for these tests.

Run both test files -- they MUST fail.

GREEN phase:
`src/game/systems/PuzzleEngine.ts`:
Import GameState, PuzzleCondition, PuzzleAction, PuzzleDefinition, EventBus. Implement:
- Constructor gets GameState.getInstance()
- evaluateConditions(conditions: PuzzleCondition[]): boolean -- conditions.every(c => evaluateCondition(c))
- evaluateCondition: switch on type, checking GameState methods
- executeActions(actions: PuzzleAction[]): string -- iterate actions, collect message strings
- executeAction: switch on type, mutating GameState or emitting events
- tryPuzzle(verb, subject, target, puzzles): find first matching puzzle where trigger matches AND conditions pass, execute its actions, mark once-puzzles solved via flag `puzzle-solved:{id}`
- matchesTrigger: check verb, subject, target, and once-solved flag

For 'trigger-death' action: `EventBus.emit('trigger-death', action.deathId)`.
For 'remove-hotspot', 'add-hotspot', 'open-exit': `EventBus.emit('room-update', action)`.

`src/game/state/SaveManager.ts`:
Static methods only (no instance needed). Constants: SAVE_PREFIX='kqgame', AUTO_SAVE_KEY, MANUAL_SAVE_PREFIX, MAX_SAVE_SLOTS=5.
- autoSave(state): localStorage.setItem for data + meta (timestamp, roomId)
- loadAutoSave(state): get + deserialize, return boolean
- hasAutoSave(): check localStorage
- saveToSlot(state, slot): write data + meta (slot, timestamp, roomId, playTimeMs)
- loadFromSlot(state, slot): get + deserialize, return boolean
- getSlotInfos(): iterate 1-5, parse meta or null
- Wrap all localStorage calls in try/catch, return false / show console.warn on failure

SaveSlotInfo interface: { slot, roomId, timestamp, playTimeMs }.

Run tests -- they MUST pass.

REFACTOR: Ensure consistent error handling in SaveManager. Clean up PuzzleEngine switch statements.
  </action>
  <verify>Run `npx vitest run src/game/__tests__/PuzzleEngine.test.ts src/game/__tests__/SaveManager.test.ts` -- all tests pass.</verify>
  <done>PuzzleEngine evaluates conditions against GameState and executes actions. SaveManager persists to localStorage with auto-save and 5 manual slots. All test cases pass for both modules.</done>
</task>

</tasks>

<verification>
Run full test suite: `npx vitest run` -- all tests pass (Phase 3 parser tests + new Phase 4 tests).
Verify no TypeScript errors: `npx tsc --noEmit`.
</verification>

<success_criteria>
- GameState singleton with inventory, flags, rooms, serialization -- fully tested
- PuzzleEngine evaluates data-driven conditions and executes actions -- fully tested
- SaveManager handles auto-save and 5 manual save slots via localStorage -- fully tested
- Type definitions (PuzzleCondition, PuzzleAction, PuzzleDefinition, ItemDefinition, GameStateData) exported and importable
- All existing Phase 3 parser tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-gameplay-systems/04-01-SUMMARY.md`
</output>
