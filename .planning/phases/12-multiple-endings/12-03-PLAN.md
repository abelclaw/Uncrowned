---
phase: 12-multiple-endings
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/game/scenes/EndingsGalleryScene.ts
  - src/game/scenes/MainMenuScene.ts
  - src/game/main.ts
  - public/assets/data/rooms/dungeon.json
  - public/assets/data/rooms/clock_tower.json
  - public/assets/data/rooms/mirror_hall.json
autonomous: true
requirements: [ENDS-04, ENDS-05]

must_haves:
  truths:
    - "Player who has discovered at least one ending sees Endings Gallery option in main menu"
    - "EndingsGalleryScene shows all 4 endings with discovered titles and locked hints"
    - "Key decision-point puzzles in dungeon, clock_tower, and mirror_hall have narrator text hinting at ending significance"
  artifacts:
    - path: "src/game/scenes/EndingsGalleryScene.ts"
      provides: "Endings gallery scene showing discovered/locked endings"
      exports: ["EndingsGalleryScene"]
    - path: "src/game/scenes/MainMenuScene.ts"
      provides: "Conditional Endings Gallery menu item"
      contains: "Endings Gallery"
    - path: "public/assets/data/rooms/dungeon.json"
      provides: "Ending-influence narrator text in clerk confrontation puzzles"
      contains: "endingInfluence"
  key_links:
    - from: "src/game/scenes/MainMenuScene.ts"
      to: "src/game/scenes/EndingsGalleryScene.ts"
      via: "scene.start('EndingsGalleryScene') conditional on MetaGameState"
      pattern: "scene\\.start\\('EndingsGalleryScene'"
    - from: "src/game/scenes/EndingsGalleryScene.ts"
      to: "endings-registry.json"
      via: "cache.json.get('endings-registry')"
      pattern: "cache\\.json\\.get\\('endings-registry'\\)"
---

<objective>
Create the Endings Gallery UI and add ending-influence markers to key decision-point rooms.

Purpose: Players can browse their discovered endings in a gallery (paralleling the Death Gallery), and key decision points throughout the game now have subtle narrator cues that signal the choice matters for the ending. This completes the meta-progression loop and makes the branching visible to players.

Output: EndingsGalleryScene.ts, MainMenuScene updated with gallery link, main.ts updated with scene registration, and 3 room JSONs annotated with ending-influence metadata.
</objective>

<execution_context>
@/Users/abel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multiple-endings/12-RESEARCH.md
@.planning/phases/12-multiple-endings/12-01-SUMMARY.md
@src/game/scenes/DeathGalleryScene.ts
@src/game/scenes/MainMenuScene.ts
@src/game/main.ts
@src/game/state/MetaGameState.ts
@src/game/types/EndingRegistryData.ts
@public/assets/data/rooms/dungeon.json
@public/assets/data/rooms/clock_tower.json
@public/assets/data/rooms/mirror_hall.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EndingsGalleryScene and wire from MainMenuScene + main.ts</name>
  <files>src/game/scenes/EndingsGalleryScene.ts, src/game/scenes/MainMenuScene.ts, src/game/main.ts</files>
  <action>
Create EndingsGalleryScene.ts following the DeathGalleryScene pattern but SIMPLER (only 4 entries, no pagination needed).

**EndingsGalleryScene layout:**
- Dark background (960x540, #1a1a2e)
- Title: "Endings Gallery" at y~60, 26px monospace, #c8c8d4
- Subtitle: "{discovered}/{total} endings discovered" at y~90, 12px monospace, #888888
- 4 ending cards arranged vertically (no grid needed -- 4 entries fit in a list)
- Each card: y positions at ~150, ~230, ~310, ~390 (80px spacing)

**For each ending entry in endings-registry.json:**
- If discovered (MetaGameState.getEndingsDiscovered() includes the id):
  - Title text: ending.title, 18px monospace, #c8c8d4
  - Subtitle text: ending.subtitle, 11px monospace, #888888, below title
  - Make card interactive -- on click, show detail overlay with full epilogue
- If NOT discovered:
  - Title text: "???", 18px monospace, #555555
  - Hint text: ending.galleryHint, 11px monospace, #555555, below title (italic style via word choice, not CSS)

**Detail overlay (on click of discovered ending):**
- Semi-transparent dark rectangle overlay (960x540, alpha 0.9)
- Ending title at y~80, 22px
- Subtitle at y~110, 12px
- Full epilogue text at y~300 (centered), 12px monospace, #c8c8d4, wordWrap width 780, lineSpacing 5
- "[ Back ]" button at y~490 to close overlay and return to gallery list
- The overlay should setInteractive() to capture clicks and prevent pass-through

**"[ Back ]" button at bottom of gallery (y~470):**
- Returns to the scene specified by data.returnTo (default: 'MainMenuScene')
- Same hover pattern as all menu items

**Accept scene data:**
```typescript
interface EndingsGalleryData {
    returnTo?: string;  // defaults to 'MainMenuScene'
}
```

**MainMenuScene.ts changes:**
After the existing Death Gallery conditional block (which checks `MetaGameState.getInstance().getDeathsDiscovered().length > 0`), add a similar block:

```typescript
// Endings Gallery -- only visible when at least 1 ending discovered
if (MetaGameState.getInstance().getEndingsDiscovered().length > 0) {
    const endingsText = this.createMenuItem('Endings Gallery', 480, y, () => {
        this.scene.start('EndingsGalleryScene', { returnTo: 'MainMenuScene' });
    });
    this.menuItems.push(endingsText);
    y += 50;
}
```

IMPORTANT: The Death Gallery block currently does NOT increment y after adding its menu item. You need to check the current code carefully. The Death Gallery item is the last item in showMainMenu(), so y was not incremented. Now that Endings Gallery comes after it, you must ensure y is properly tracked. Add `y += 50;` after the Death Gallery push if not already there, then add the Endings Gallery block.

**main.ts changes:**
Import EndingsGalleryScene and add to scene array:
```typescript
import { EndingsGalleryScene } from './scenes/EndingsGalleryScene';
// In scene array (after EndingScene):
scene: [..., EndingScene, EndingsGalleryScene],
```

Note: Plan 12-02 also modifies main.ts to add EndingScene. If 12-02 executes first, EndingScene is already in the array -- just append EndingsGalleryScene. If 12-03 executes first (both are Wave 2), add EndingsGalleryScene after DeathGalleryScene and note that 12-02 will add EndingScene separately. Check the current state of main.ts before modifying.
  </action>
  <verify>npx tsc --noEmit -- should compile. Verify EndingsGalleryScene is in main.ts scene array. Verify MainMenuScene conditionally shows "Endings Gallery" when getEndingsDiscovered().length > 0.</verify>
  <done>EndingsGalleryScene displays a vertical list of 4 endings (discovered show title/subtitle with detail overlay; locked show hints). MainMenuScene conditionally links to EndingsGalleryScene. Scene registered in main.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Add ending-influence metadata and narrator cues to key decision-point rooms</name>
  <files>public/assets/data/rooms/dungeon.json, public/assets/data/rooms/clock_tower.json, public/assets/data/rooms/mirror_hall.json</files>
  <action>
Add an `endingInfluence` boolean field to key decision-point puzzles in room JSONs, and enhance the narrator-say text in those puzzles to subtly signal that the choice matters for the ending.

**dungeon.json changes:**
Find the puzzle that sets `clerk_remembers` (the show-memory-crystal or equivalent puzzle where the player shows the crystal to the Clerk). Add `"endingInfluence": true` to the puzzle definition object (at the same level as id, trigger, conditions, actions, once).

Find the puzzle that sets `clerk_outwitted` (the outwit-clerk or equivalent puzzle where the player uses the contract clauses). Add `"endingInfluence": true` to that puzzle definition too.

For both puzzles, check the existing narrator-say text. If the narrator text does NOT already hint at the significance of the choice, append a sentence to the narrator-say text. For the clerk_remembers path, add something like: "Something shifts in the air. The kind of shift that changes how stories end." For the clerk_outwitted path, add something like: "The Clerk's cooperation, such as it is, feels like it will echo. Some choices shape more than the moment."

Only add these sentences if the existing narrator text does not already contain ending-significance language. Read the existing text first.

**clock_tower.json changes:**
Find the puzzle that sets `clock-fixed` (the repair-clock or equivalent). Add `"endingInfluence": true`. The clock-fixed flag is a secondary ending detail (adds a line to the epilogue about the clock running on time). The existing narrator text for clock repair is likely satisfying on its own -- add a subtle cue only if the current text has no sense of lasting consequence. Something like: "The clock ticks. For the first time in centuries, something in this kingdom is precisely on time. That feels like it matters."

**mirror_hall.json changes:**
Find the puzzle that sets `mirror_truth_revealed`. Add `"endingInfluence": true`. Similar to clock_tower -- add a subtle narrator cue if not already present. Something like: "The mirror's truth, once seen, cannot be unseen. Some truths follow you to the end."

IMPORTANT NOTES:
- The `endingInfluence` field is documentation/metadata only. PuzzleEngine does NOT read it. It exists for content authors and potentially future UI features.
- Do NOT change any puzzle trigger, conditions, or non-narrator actions. Only add the endingInfluence field and optionally enhance narrator-say text.
- Read each room JSON fully before making changes to understand existing narrator text.
- The PuzzleDefinition interface in PuzzleData.ts does NOT need updating for this optional field -- JSON can have extra fields that TypeScript ignores.
  </action>
  <verify>Verify dungeon.json, clock_tower.json, and mirror_hall.json are all valid JSON after changes. Check that endingInfluence: true appears on the correct puzzles. Verify no puzzle triggers or conditions were accidentally modified.</verify>
  <done>Key decision-point puzzles across dungeon, clock_tower, and mirror_hall have endingInfluence: true metadata and narrator text that subtly cues the player about the significance of their choice for the ending.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- EndingsGalleryScene registered in main.ts
- MainMenuScene shows "Endings Gallery" conditionally (only when endings discovered > 0)
- EndingsGalleryScene loads ending data from cache and displays 4 entries
- dungeon.json has endingInfluence: true on clerk_remembers and clerk_outwitted puzzles
- clock_tower.json has endingInfluence: true on clock-fixed puzzle
- mirror_hall.json has endingInfluence: true on mirror_truth_revealed puzzle
- All modified JSON files are valid JSON
</verification>

<success_criteria>
Players can browse discovered endings in a gallery UI accessible from the main menu. Key decision points throughout the game have narrator cues hinting at ending significance, fulfilling the "visible ending-influence moments" requirement. The meta-progression loop is complete: play -> discover ending -> view in gallery -> replay for different ending.
</success_criteria>

<output>
After completion, create `.planning/phases/12-multiple-endings/12-03-SUMMARY.md`
</output>
