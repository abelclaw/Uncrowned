---
phase: 09-art-pipeline-schema-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - src/game/scenes/Preloader.ts
  - src/game/scenes/RoomScene.ts
  - src/game/types/RoomData.ts
  - public/assets/data/rooms/forest_clearing.json
  - public/assets/data/rooms/cave_entrance.json
  - public/assets/data/rooms/village_path.json
  - public/assets/data/rooms/cave_depths.json
  - public/assets/data/rooms/underground_pool.json
  - public/assets/data/rooms/village_square.json
  - public/assets/data/rooms/old_watchtower.json
  - public/assets/data/rooms/forest_bridge.json
  - public/assets/data/rooms/castle_courtyard.json
  - public/assets/data/rooms/castle_hallway.json
  - public/assets/data/rooms/throne_room.json
  - public/assets/data/rooms/royal_kitchen.json
  - public/assets/data/rooms/castle_garden.json
  - public/assets/data/rooms/servants_quarters.json
  - public/assets/data/rooms/cavern_entrance_hall.json
  - public/assets/data/rooms/cavern_library.json
  - public/assets/data/rooms/filing_room.json
  - public/assets/data/rooms/waiting_room.json
  - public/assets/data/rooms/cavern_west_wing.json
  - public/assets/data/rooms/crystal_chamber.json
  - public/assets/data/rooms/cavern_balcony.json
  - public/assets/data/rooms/echo_chamber.json
  - public/assets/data/rooms/cavern_east_wing.json
  - public/assets/data/rooms/underground_river.json
  - public/assets/data/rooms/forge_chamber.json
  - public/assets/data/rooms/guardian_chamber.json
  - public/assets/data/rooms/petrified_forest.json
  - public/assets/data/rooms/castle_courtyard_act3.json
  - public/assets/data/rooms/throne_room_act3.json
  - public/assets/data/rooms/royal_archive.json
  - public/assets/data/rooms/wizard_tower.json
  - public/assets/data/rooms/clock_tower.json
  - public/assets/data/rooms/dungeon.json
  - public/assets/data/rooms/mirror_hall.json
  - public/assets/data/rooms/rooftop.json
  - public/assets/data/rooms/treasury.json
  - scripts/art-manifest.json
  - scripts/generate-art.ts
autonomous: false
requirements:
  - ART-02
  - ART-03
  - ART-04
  - ART-05
  - ART-06

must_haves:
  truths:
    - "All 36 rooms display unique pixel art backgrounds with no placeholder rectangles"
    - "Parallax layers render correctly: shared sky/mid per act + unique ground per room"
    - "Room backgrounds load lazily on room entry, not during initial Preloader"
    - "Game initial load completes in under 5 seconds (no upfront background loading)"
    - "Revisiting a room shows background instantly (cached in Phaser texture manager)"
    - "Player sprite displays pixel art with working idle/walk/interact animations"
    - "Item and NPC sprites display as pixel art in their correct room positions"
  artifacts:
    - path: "public/assets/backgrounds/rooms/forest_clearing.png"
      provides: "Example room-specific pixel art background"
    - path: "public/assets/backgrounds/shared/act1-sky.png"
      provides: "Example shared parallax sky layer"
    - path: "public/assets/sprites/player.png"
      provides: "Pixel art player spritesheet"
    - path: "src/game/scenes/Preloader.ts"
      provides: "Modified preloader without room background loads"
    - path: "src/game/scenes/RoomScene.ts"
      provides: "Room scene with lazy asset loading"
  key_links:
    - from: "src/game/scenes/RoomScene.ts"
      to: "public/assets/backgrounds/rooms/*.png"
      via: "loadRoomAssets() dynamically loads room-specific backgrounds"
      pattern: "this\\.load\\.image"
    - from: "public/assets/data/rooms/*.json"
      to: "public/assets/backgrounds/"
      via: "background.layers[].key references image asset keys"
      pattern: "bg-rooms-|bg-shared-"
    - from: "src/game/scenes/Preloader.ts"
      to: "public/assets/backgrounds/shared/"
      via: "Preloader loads only shared layers and starting room background"
      pattern: "bg-shared"

user_setup:
  - service: ComfyUI
    why: "Must be running locally to generate all pixel art assets"
    env_vars: []
    dashboard_config:
      - task: "Ensure ComfyUI is running at http://127.0.0.1:8188"
        location: "Launch ComfyUI Desktop or run 'python main.py' in ComfyUI directory"
---

<objective>
Generate all pixel art assets via the art pipeline, update room JSONs with new background key scheme, implement lazy loading in RoomScene, and trim Preloader to only load shared/starting assets.

Purpose: Replace all placeholder rectangle backgrounds with Flux-generated pixel art, implement per-room lazy loading for fast initial load, and update room data to reference new asset keys.
Output: 36 unique room backgrounds, 6 shared parallax layers, pixel art player spritesheet, item sprites, NPC sprites, lazy-loading RoomScene, trimmed Preloader.
</objective>

<execution_context>
@/Users/abel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-art-pipeline-schema-foundation/09-RESEARCH.md
@.planning/phases/09-art-pipeline-schema-foundation/09-02-SUMMARY.md

@src/game/scenes/Preloader.ts
@src/game/scenes/RoomScene.ts
@src/game/types/RoomData.ts
@public/assets/data/rooms/forest_clearing.json
@scripts/art-manifest.json
@scripts/style-guide.json
@scripts/generate-art.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update room JSONs, Preloader, and RoomScene for lazy loading</name>
  <files>
    src/game/scenes/Preloader.ts
    src/game/scenes/RoomScene.ts
    public/assets/data/rooms/forest_clearing.json
    public/assets/data/rooms/cave_entrance.json
    public/assets/data/rooms/village_path.json
    public/assets/data/rooms/cave_depths.json
    public/assets/data/rooms/underground_pool.json
    public/assets/data/rooms/village_square.json
    public/assets/data/rooms/old_watchtower.json
    public/assets/data/rooms/forest_bridge.json
    public/assets/data/rooms/castle_courtyard.json
    public/assets/data/rooms/castle_hallway.json
    public/assets/data/rooms/throne_room.json
    public/assets/data/rooms/royal_kitchen.json
    public/assets/data/rooms/castle_garden.json
    public/assets/data/rooms/servants_quarters.json
    public/assets/data/rooms/cavern_entrance_hall.json
    public/assets/data/rooms/cavern_library.json
    public/assets/data/rooms/filing_room.json
    public/assets/data/rooms/waiting_room.json
    public/assets/data/rooms/cavern_west_wing.json
    public/assets/data/rooms/crystal_chamber.json
    public/assets/data/rooms/cavern_balcony.json
    public/assets/data/rooms/echo_chamber.json
    public/assets/data/rooms/cavern_east_wing.json
    public/assets/data/rooms/underground_river.json
    public/assets/data/rooms/forge_chamber.json
    public/assets/data/rooms/guardian_chamber.json
    public/assets/data/rooms/petrified_forest.json
    public/assets/data/rooms/castle_courtyard_act3.json
    public/assets/data/rooms/throne_room_act3.json
    public/assets/data/rooms/royal_archive.json
    public/assets/data/rooms/wizard_tower.json
    public/assets/data/rooms/clock_tower.json
    public/assets/data/rooms/dungeon.json
    public/assets/data/rooms/mirror_hall.json
    public/assets/data/rooms/rooftop.json
    public/assets/data/rooms/treasury.json
  </files>
  <action>
    **Update all 36 room JSON files** to use new background key scheme:
    - Replace the current 4-layer shared background structure with 3-layer act-specific structure
    - Each room gets: (1) shared act sky, (2) shared act mid-ground, (3) unique room ground

    For Act 1 rooms (forest_clearing, cave_entrance, village_path, cave_depths, underground_pool, village_square, old_watchtower, forest_bridge, castle_courtyard, castle_hallway, throne_room, royal_kitchen, castle_garden, servants_quarters):
    ```json
    "background": {
      "layers": [
        { "key": "bg-shared-act1-sky", "scrollFactor": 0 },
        { "key": "bg-shared-act1-mid", "scrollFactor": 0.3 },
        { "key": "bg-rooms-{roomId}", "scrollFactor": 1 }
      ],
      "worldWidth": <keep existing value>
    }
    ```

    For Act 2 rooms (cavern_entrance_hall through guardian_chamber): same pattern with "act2".
    For Act 3 rooms (petrified_forest through treasury): same pattern with "act3".

    Replace `{roomId}` with the actual room ID in each file (e.g., `bg-rooms-forest_clearing`).

    IMPORTANT: Only change the `background.layers` array. Do NOT touch any other fields (walkableArea, exits, hotspots, items, puzzles, etc.).

    **Update `src/game/scenes/Preloader.ts`:**
    - REMOVE the 4 shared placeholder background loads (bg-sky, bg-mountains, bg-trees, bg-ground)
    - ADD shared parallax layers for the starting act (Act 1): `bg-shared-act1-sky` and `bg-shared-act1-mid`
    - ADD the starting room's ground layer: `bg-rooms-forest_clearing`
    - Keep ALL other loads unchanged (player spritesheet, room JSONs, items, NPCs, dialogue, audio)
    - The paths map as follows:
      - `bg-shared-act1-sky` -> `assets/backgrounds/shared/act1-sky.png`
      - `bg-shared-act1-mid` -> `assets/backgrounds/shared/act1-mid.png`
      - `bg-rooms-forest_clearing` -> `assets/backgrounds/rooms/forest_clearing.png`

    **Update `src/game/scenes/RoomScene.ts`:**
    - Add a private `loadRoomAssets(): Promise<void>` method that:
      - Iterates over `this.roomData.background.layers`
      - For each layer, checks `this.textures.exists(layer.key)` -- skip if already cached
      - For uncached keys, determines the file path from the key convention:
        - Keys starting with `bg-shared-` -> `assets/backgrounds/shared/{rest}.png` (e.g., `bg-shared-act1-sky` -> `assets/backgrounds/shared/act1-sky.png`)
        - Keys starting with `bg-rooms-` -> `assets/backgrounds/rooms/{rest}.png` (e.g., `bg-rooms-forest_clearing` -> `assets/backgrounds/rooms/forest_clearing.png`)
      - ALSO loads item sprites for items present in the room (not already removed):
        - For each `roomData.items[]` where item ID is NOT in `GameState.removedItems[roomId]`:
          - Key: `item-{itemId}`, Path: `assets/sprites/items/{itemId}.png`
      - ALSO loads NPC sprites for NPCs present in the room:
        - For each `roomData.npcs[]`:
          - Key: `npc-{npcId}`, Path: `assets/sprites/npcs/{npcId}.png`
      - Calls `this.load.image(key, path)` for each uncached asset
      - If any assets need loading: `this.load.once(Phaser.Loader.Events.COMPLETE, resolve); this.load.start()`
      - If all cached: resolve immediately

    - Modify `create()` to gate background AND sprite rendering on lazy load:
      - At the very start of create(), before background rendering (step 1), show a brief loading text:
        ```typescript
        const loadingText = this.add.text(480, 270, 'Loading...', {
            fontFamily: 'monospace', fontSize: '14px', color: '#ffffff'
        }).setOrigin(0.5).setScrollFactor(0).setDepth(999);
        ```
      - Call `this.loadRoomAssets().then(() => { ... })` wrapping the background layer creation code AND item/NPC sprite rendering
      - Inside the then callback: destroy loadingText, create background layers as before
      - ALSO render item sprites: for each non-removed item in `roomData.items[]`, add a sprite image at the item's zone center position:
        ```typescript
        const itemSprite = this.add.image(
            item.zone.x + item.zone.width / 2,
            item.zone.y + item.zone.height / 2,
            `item-${item.id}`
        ).setDepth(5);
        ```
        Store these sprites in a map so they can be destroyed when items are taken.
      - ALSO render NPC sprites: for each NPC in `roomData.npcs[]`, add a sprite image at the NPC's zone center:
        ```typescript
        const npcSprite = this.add.image(
            npc.zone.x + npc.zone.width / 2,
            npc.zone.y + npc.zone.height / 2,
            `npc-${npc.id}`
        ).setDepth(5);
        ```
      - Move ONLY the background layer creation + sprite rendering (step 1) inside the async callback. Everything else (navigation, player, exits, hotspots, camera, input, events) stays synchronous in create() since they don't depend on loaded textures for hit detection.

    - When an item is taken (already handled by existing item take logic), also destroy its sprite from the sprite map.

    - IMPORTANT: The loading text will only show for 100-500ms on the first visit to a room. On revisits, textures are cached and it resolves instantly. The existing fade-in transition (500ms) will largely mask this load time.
  </action>
  <verify>
    Run `npx tsc --noEmit` -- no type errors.
    Verify all 36 room JSONs have been updated: `node -e "const fs=require('fs'); const rooms=fs.readdirSync('public/assets/data/rooms').filter(f=>f.endsWith('.json')); let ok=0; rooms.forEach(r=>{const d=JSON.parse(fs.readFileSync('public/assets/data/rooms/'+r));if(d.background.layers.some(l=>l.key.startsWith('bg-rooms-')||l.key.startsWith('bg-shared-')))ok++}); console.log(ok+'/'+rooms.length+' rooms updated')"` -- should print 36/36.
    Verify Preloader no longer loads bg-sky/bg-mountains/bg-trees/bg-ground.
    Verify RoomScene has loadRoomAssets method.
  </verify>
  <done>
    - All 36 room JSONs use new 3-layer key scheme (bg-shared-actN-sky, bg-shared-actN-mid, bg-rooms-roomId)
    - Preloader loads only shared Act 1 layers + starting room background (not all 4 placeholders)
    - RoomScene lazily loads room-specific backgrounds, item sprites, and NPC sprites on entry via loadRoomAssets()
    - Background rendering AND item/NPC sprite rendering are gated behind async load completion
    - Item sprites render at zone center positions and are destroyed when items are taken
    - NPC sprites render at zone center positions
    - Loading text shown briefly during first room visit
    - All other Preloader assets unchanged (player, JSON, audio, dialogue)
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate all pixel art assets and populate sprite manifest entries</name>
  <files>
    scripts/art-manifest.json
    scripts/generate-art.ts
  </files>
  <action>
    This task generates all pixel art assets using the art pipeline from Plan 09-02. It requires ComfyUI running locally.

    **Step 1: Create output directories:**
    ```bash
    mkdir -p public/assets/backgrounds/shared
    mkdir -p public/assets/backgrounds/rooms
    mkdir -p public/assets/sprites/items
    mkdir -p public/assets/sprites/npcs
    ```

    **Step 2: Populate item and NPC entries in art-manifest.json:**

    Read `public/assets/data/items.json` to get all 37 item IDs and names. Add each to `items` section of art-manifest.json:
    ```json
    "items": {
      "rusty-key": {
        "prompt": "old rusty iron key, medieval fantasy",
        "seed": 400001,
        "output": "public/assets/sprites/items/rusty-key.png",
        "dimensions": { "width": 32, "height": 32 }
      }
    }
    ```
    Craft a brief visual prompt for each item based on its name and description in items.json. Assign seeds 400001-400037.

    Read `public/assets/data/npcs/npcs.json` to get all 11 NPC IDs. Add each to `npcs` section:
    ```json
    "npcs": {
      "old_man": {
        "prompt": "elderly wise man with long beard, traveler robes, walking staff, pixel art character",
        "seed": 500001,
        "output": "public/assets/sprites/npcs/old_man.png",
        "dimensions": { "width": 48, "height": 64 }
      }
    }
    ```
    Craft a visual prompt for each NPC based on their personality/description in npcs.json. Assign seeds 500001-500011.

    **Step 3: Generate shared backgrounds (6 images):**
    ```bash
    npx tsx scripts/generate-art.ts --type shared
    ```

    **Step 4: Generate all room backgrounds (36 images):**
    ```bash
    npx tsx scripts/generate-art.ts --type backgrounds
    ```

    **Step 5: Generate player sprite:**
    Add a `--type player` handler to generate-art.ts (or use `--type sprites`).
    The player sprite needs special handling: generate base poses at 1024x1024, then use sharp to assemble into a 768x64 spritesheet (16 frames of 48x64). This may require generating multiple images (idle, walk, interact) and compositing them.

    Alternative approach if full spritesheet generation proves difficult: Generate a single standing character image and use sharp to create a simple 16-frame spritesheet with minor variations (mirrored, slight crop shifts). The exact animation frames can be refined later.

    **Step 6: Generate item sprites (37 images):**
    Run `npx tsx scripts/generate-art.ts --type items` (add this handler to the script).
    Post-process each with sharp: ensureAlpha(), resize to 32x32 with transparent background.

    **Step 7: Generate NPC sprites (11 images):**
    Run `npx tsx scripts/generate-art.ts --type npcs` (add this handler to the script).
    Post-process each with sharp: ensureAlpha(), resize to 48x64 with transparent background.

    **NOTE:** If ComfyUI is unavailable or generation fails, create placeholder PNGs at correct dimensions using sharp (solid colored rectangles with text overlay). The lazy loading and room JSON changes still work with placeholder images. The pipeline can be re-run when ComfyUI is available.

    **IMPORTANT for generate-art.ts updates:** Add `--type items`, `--type npcs`, `--type player`, and `--type sprites` (all non-background types) to the CLI handler. Each type reads its section from art-manifest.json and calls the appropriate generate + process function.
  </action>
  <verify>
    Verify shared backgrounds exist: `ls public/assets/backgrounds/shared/` -- should show 6 PNG files (act1-sky, act1-mid, act2-sky, act2-mid, act3-sky, act3-mid).
    Verify room backgrounds exist: `ls public/assets/backgrounds/rooms/ | wc -l` -- should show 36 PNG files.
    Verify player sprite exists: `ls public/assets/sprites/player.png`
    Verify item sprites: `ls public/assets/sprites/items/ | wc -l` -- should show 37 PNG files.
    Verify NPC sprites: `ls public/assets/sprites/npcs/ | wc -l` -- should show 11 PNG files.
    Verify a room background dimensions: `node -e "const sharp=require('sharp');sharp('public/assets/backgrounds/rooms/forest_clearing.png').metadata().then(m=>console.log(m.width+'x'+m.height))"` -- should print 1920x540 (or 960x540 for standard-width rooms).
  </verify>
  <done>
    - 6 shared parallax backgrounds generated (2 per act: sky + mid)
    - 36 unique room ground-layer backgrounds generated
    - Player spritesheet generated at 768x64 (16 frames of 48x64)
    - 37 item sprites generated at 32x32 with transparency
    - 11 NPC sprites generated at 48x64 with transparency
    - Art manifest updated with item and NPC entries
    - generate-art.ts supports --type items/npcs/player/sprites
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete pixel art integration</name>
  <action>
    Complete pixel art integration built: all 36 rooms display Flux-generated pixel art backgrounds with lazy loading, parallax layers render per-act, player/item/NPC sprites are pixel art. Human verifies visual quality across rooms and acts.
  </action>
  <verify>
    1. Run `npm run dev` and open the game in browser
    2. Verify initial load is fast (under 5 seconds) -- observe Preloader progress bar
    3. Start a new game -- forest_clearing should show pixel art background (not placeholder rectangles)
    4. Navigate to cave_entrance -- verify brief loading then pixel art background appears
    5. Return to forest_clearing -- verify instant background (cached, no loading text)
    6. Navigate to a room in a different act (e.g., cavern_entrance_hall) -- verify different sky/mid parallax layers
    7. Check player sprite renders with working animations (idle, walk on click, interact on hotspot)
    8. Open browser DevTools Network tab -- verify room backgrounds load on-demand (not all upfront)
    9. Check console for any "Texture missing" warnings -- should be zero
    10. Spot-check 3-4 rooms across different acts for art style consistency
  </verify>
  <done>User approves pixel art integration across all rooms, or provides feedback for visual adjustments</done>
</task>

</tasks>

<verification>
1. All 36 room JSONs reference new key scheme (bg-rooms-*, bg-shared-*)
2. Preloader loads only starting room + shared Act 1 assets (not all backgrounds)
3. RoomScene.loadRoomAssets() dynamically loads missing textures on room entry
4. `npm run dev` shows pixel art in every room with no placeholder rectangles
5. Initial load under 5 seconds (measured in DevTools)
6. No "Texture missing" console warnings
7. Player, item, and NPC sprites display as pixel art
8. `npx tsc --noEmit` passes
9. Parallax scrolling still works with new 3-layer structure
</verification>

<success_criteria>
- Player sees consistent pixel art backgrounds in every room -- no placeholder rectangles remain
- Player character, items, and NPCs display as pixel art sprites
- Game initial load completes in under 5 seconds on broadband
- Rooms load their art on entry (lazy), not upfront
- Parallax effect preserved with shared sky/mid + unique ground per room
- Art style is consistent across rooms within each act
</success_criteria>

<output>
After completion, create `.planning/phases/09-art-pipeline-schema-foundation/09-03-SUMMARY.md`
</output>
