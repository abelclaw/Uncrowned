---
phase: 09-art-pipeline-schema-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/game/state/GameStateTypes.ts
  - src/game/state/GameState.ts
  - src/game/state/SaveManager.ts
  - src/game/state/MetaGameState.ts
  - src/game/state/migrations/index.ts
  - src/game/state/migrations/v1-to-v2.ts
  - src/game/__tests__/GameState.test.ts
  - src/game/__tests__/SaveManager.test.ts
  - src/game/__tests__/MetaGameState.test.ts
  - src/game/__tests__/migrations.test.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "v1.0 saves (no version field) load correctly in v2.0 build with all existing data preserved"
    - "New saves include version:2 field and all existing fields"
    - "MetaGameState persists in kqgame-meta localStorage key independently from GameState save slots"
    - "MetaGameState survives new-game reset and save slot loads"
    - "Export produces a downloadable JSON file containing both GameState and MetaGameState"
    - "Import restores both GameState and MetaGameState from a previously exported JSON file"
  artifacts:
    - path: "src/game/state/GameStateTypes.ts"
      provides: "GameStateData interface with version field, CURRENT_SAVE_VERSION constant"
      contains: "version: number"
    - path: "src/game/state/migrations/v1-to-v2.ts"
      provides: "v1->v2 migration function"
      exports: ["migrateV1toV2"]
    - path: "src/game/state/migrations/index.ts"
      provides: "Migration registry and migrate() function"
      exports: ["migrate", "CURRENT_SAVE_VERSION"]
    - path: "src/game/state/MetaGameState.ts"
      provides: "Cross-playthrough persistence singleton"
      exports: ["MetaGameState", "MetaGameStateData"]
    - path: "src/game/state/SaveManager.ts"
      provides: "Save/load with migration, export/import methods"
      exports: ["SaveManager"]
    - path: "src/game/state/GameState.ts"
      provides: "GameState with migration-aware deserialize"
  key_links:
    - from: "src/game/state/GameState.ts"
      to: "src/game/state/migrations/index.ts"
      via: "deserialize() calls migrate()"
      pattern: "migrate\\("
    - from: "src/game/state/SaveManager.ts"
      to: "src/game/state/MetaGameState.ts"
      via: "exportSave/importSave include MetaGameState data"
      pattern: "MetaGameState"
    - from: "src/game/state/migrations/index.ts"
      to: "src/game/state/migrations/v1-to-v2.ts"
      via: "migration registry references v1->v2 function"
      pattern: "migrateV1toV2"
---

<objective>
Add save schema versioning with v1->v2 migration, MetaGameState cross-playthrough persistence, and save export/import functionality.

Purpose: v1.0 saves must load safely in v2.0, cross-playthrough data (death gallery, endings) needs a persistence layer independent of save slots, and players need to backup/transfer saves between browsers.
Output: Versioned GameStateData, migration chain, MetaGameState singleton, SaveManager export/import methods, comprehensive test suite.
</objective>

<execution_context>
@/Users/abel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-art-pipeline-schema-foundation/09-RESEARCH.md

@src/game/state/GameStateTypes.ts
@src/game/state/GameState.ts
@src/game/state/SaveManager.ts
@src/game/__tests__/GameState.test.ts
@src/game/__tests__/SaveManager.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema versioning, migration chain, and MetaGameState (TDD)</name>
  <files>
    src/game/state/GameStateTypes.ts
    src/game/state/GameState.ts
    src/game/state/migrations/index.ts
    src/game/state/migrations/v1-to-v2.ts
    src/game/state/MetaGameState.ts
    src/game/__tests__/GameState.test.ts
    src/game/__tests__/MetaGameState.test.ts
    src/game/__tests__/migrations.test.ts
  </files>
  <action>
    **RED phase -- Write failing tests first:**

    1. Create `src/game/__tests__/migrations.test.ts`:
       - Test: `migrate()` on a v1 save (no version field) returns object with `version: 2` and all original fields preserved
       - Test: `migrate()` on a v1 save with missing optional fields fills defaults (empty arrays, 0 numbers)
       - Test: `migrate()` on a v2 save (version: 2) returns it unchanged (no-op)
       - Test: `migrate()` throws on unknown future version (version: 99)
       - Test: `migrateV1toV2()` specifically adds version: 2 and preserves inventory, flags, etc.

    2. Add tests to `src/game/__tests__/GameState.test.ts`:
       - Test: `serialize()` output includes `version: 2`
       - Test: `deserialize()` on v1 JSON (no version field) succeeds and state has `version: 2`
       - Test: `deserialize()` on v2 JSON works normally
       - Test: `reset()` produces state with `version: 2`
       - Test: `getData().version` equals `CURRENT_SAVE_VERSION`

    3. Create `src/game/__tests__/MetaGameState.test.ts` (use same localStorage mock pattern from SaveManager.test.ts):
       - Test: `getInstance()` returns same instance (singleton)
       - Test: `recordDeath("death-1")` returns true on first call, false on second (idempotent)
       - Test: `getDeathsDiscovered()` returns previously recorded deaths
       - Test: `recordEnding("ending-1")` returns true on first call, false on second
       - Test: `getEndingsDiscovered()` returns previously recorded endings
       - Test: `getData()` returns full MetaGameStateData object
       - Test: `save()` persists to localStorage key `kqgame-meta`
       - Test: After `save()`, new instance loads persisted data (simulate via direct localStorage read)
       - Test: `reset()` clears all data but keeps version

    **GREEN phase -- Implement to pass tests:**

    4. Update `src/game/state/GameStateTypes.ts`:
       - Add `version: number` to `GameStateData` interface
       - Export `CURRENT_SAVE_VERSION = 2`
       - Update `getDefaultState()` to include `version: CURRENT_SAVE_VERSION`

    5. Create `src/game/state/migrations/v1-to-v2.ts`:
       - Export `migrateV1toV2(data: Record<string, unknown>): Record<string, unknown>`
       - Add `version: 2`, preserve all existing fields with fallback defaults for any missing ones
       - Explicitly list every field: currentRoom (default 'forest_clearing'), inventory ([]), flags ({}), visitedRooms ([]), removedItems ({}), playTimeMs (0), deathCount (0), dialogueStates ({})

    6. Create `src/game/state/migrations/index.ts`:
       - Export `CURRENT_SAVE_VERSION` re-exported from GameStateTypes
       - Define `MigrationFn = (data: Record<string, unknown>) => Record<string, unknown>`
       - Define `migrations: Record<number, MigrationFn>` map with `1: migrateV1toV2`
       - Export `migrate(raw: Record<string, unknown>): GameStateData` function:
         - Detect version: `raw.version ?? 1` (no version field = v1)
         - If version === CURRENT_SAVE_VERSION, return as-is (cast to GameStateData)
         - If version > CURRENT_SAVE_VERSION, throw Error("Save from newer version")
         - Loop: while version < CURRENT_SAVE_VERSION, apply migrations[version], increment

    7. Update `src/game/state/GameState.ts`:
       - Import `migrate` from `./migrations/index.ts`
       - Change `deserialize()` from `this.data = JSON.parse(json)` to `this.data = migrate(JSON.parse(json))`
       - Change `serialize()` to ensure version is always present (it will be, since data always has version after migration or reset)

    8. Create `src/game/state/MetaGameState.ts`:
       - Export `MetaGameStateData` interface: `{ version: number; deathsDiscovered: string[]; endingsDiscovered: string[] }`
       - Export `MetaGameState` class (singleton pattern matching GameState):
         - Private static instance, private data: MetaGameStateData
         - `META_KEY = 'kqgame-meta'` constant
         - Constructor loads from localStorage (try/catch, fallback to defaults)
         - `getInstance()`, `save()` (writes to localStorage), `reset()` (clears data, saves)
         - `recordDeath(deathId: string): boolean` -- returns true if new, false if duplicate; auto-saves
         - `recordEnding(endingId: string): boolean` -- same pattern
         - `getDeathsDiscovered(): readonly string[]`
         - `getEndingsDiscovered(): readonly string[]`
         - `getData(): Readonly<MetaGameStateData>` -- returns full data for export

    **REFACTOR phase:** Ensure all tests pass, clean up any type issues.

    **Important:** MetaGameState is a separate singleton -- it does NOT participate in GameState.serialize()/deserialize(). It uses its own localStorage key. The singleton needs a `resetInstance()` static method for testing (allows creating fresh instances in tests).
  </action>
  <verify>
    Run `npx vitest run src/game/__tests__/migrations.test.ts src/game/__tests__/GameState.test.ts src/game/__tests__/MetaGameState.test.ts` -- all tests pass.
    Run `npx vitest run` -- all existing tests still pass (no regressions).
    Run `npx tsc --noEmit` -- no type errors.
  </verify>
  <done>
    - v1 saves (no version field) migrate to v2 with all data preserved
    - v2 saves pass through unchanged
    - GameState.serialize() includes version: 2
    - GameState.deserialize() runs migration chain automatically
    - MetaGameState singleton persists to kqgame-meta localStorage key
    - MetaGameState.recordDeath/recordEnding are idempotent and auto-save
    - All new and existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Save export/import with migration support (TDD)</name>
  <files>
    src/game/state/SaveManager.ts
    src/game/__tests__/SaveManager.test.ts
  </files>
  <action>
    **RED phase -- Write failing tests first:**

    1. Add tests to `src/game/__tests__/SaveManager.test.ts`:

       Export tests:
       - Test: `SaveManager.createExportData(state, meta)` returns object with format "kqgame-save", version 1, exportedAt string, gameState object, metaState object
       - Test: Export data gameState contains all current GameState fields including version: 2
       - Test: Export data metaState contains MetaGameState data

       Import tests:
       - Test: `SaveManager.parseImportData(jsonString)` returns parsed { gameState, metaState } on valid data
       - Test: `parseImportData()` throws on invalid JSON
       - Test: `parseImportData()` throws on JSON missing "format" field
       - Test: `parseImportData()` throws on JSON with wrong format value
       - Test: `parseImportData()` throws on JSON missing gameState or metaState
       - Test: Parsed gameState from import runs through migration (v1 import data without version field gets version: 2 after load)

       Migration-aware load tests:
       - Test: `loadAutoSave()` on a v1-format save (no version field in localStorage) succeeds and resulting state has version: 2
       - Test: `loadFromSlot()` on a v1-format save succeeds and resulting state has version: 2

    **GREEN phase -- Implement to pass tests:**

    2. Update `src/game/state/SaveManager.ts`:
       - Import MetaGameState from `./MetaGameState.ts`
       - Import type MetaGameStateData from `./MetaGameState.ts`

       Add static method `createExportData(state: GameState, meta: MetaGameState)`:
       - Returns `{ format: 'kqgame-save', version: 1, exportedAt: new Date().toISOString(), gameState: JSON.parse(state.serialize()), metaState: meta.getData() }`
       - NOTE: This is a pure data method. The actual file download (Blob + <a> click) will be triggered by UI code that calls this. Keeping it pure makes it testable.

       Add static method `exportSaveToFile(state: GameState, meta: MetaGameState): void`:
       - Calls `createExportData()`, JSON.stringify with indent 2
       - Creates Blob with type 'application/json'
       - Creates <a> element with download attribute `kqgame-save-{date}.json`
       - Clicks it, revokes URL

       Add static method `parseImportData(jsonString: string): { gameState: string; metaState: MetaGameStateData }`:
       - JSON.parse the string
       - Validate: `format === 'kqgame-save'`, gameState and metaState must exist
       - Return `{ gameState: JSON.stringify(data.gameState), metaState: data.metaState }`
       - The caller (importSaveFromFile or UI code) will call `state.deserialize(gameState)` which runs migration automatically

       Add static method `importSaveFromFile(): Promise<{ gameState: string; metaState: MetaGameStateData }>`:
       - Creates hidden `<input type="file" accept=".json">`
       - Returns Promise that resolves with parseImportData result on file read
       - Rejects on cancel, read error, or invalid format

       NOTE: `loadAutoSave()` and `loadFromSlot()` already call `state.deserialize()` which now runs migration automatically via Task 1. No changes needed to those methods -- migration is handled transparently by GameState.deserialize().

    **REFACTOR phase:** Verify all tests pass including existing ones.
  </action>
  <verify>
    Run `npx vitest run src/game/__tests__/SaveManager.test.ts` -- all tests pass (old + new).
    Run `npx vitest run` -- full suite passes.
    Run `npx tsc --noEmit` -- no type errors.
  </verify>
  <done>
    - SaveManager.createExportData() produces valid export bundle with GameState + MetaGameState
    - SaveManager.parseImportData() validates and parses import JSON with format checking
    - SaveManager.exportSaveToFile() triggers browser download of save JSON
    - SaveManager.importSaveFromFile() reads file via browser File API
    - v1 saves loaded via loadAutoSave/loadFromSlot automatically migrate to v2 (via GameState.deserialize)
    - All tests pass with no regressions
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass (migrations, GameState, MetaGameState, SaveManager)
2. `npx tsc --noEmit` -- zero type errors
3. Manual verification: create a fake v1 save in localStorage (no version field), call GameState.deserialize() on it, confirm version: 2 is present and all data intact
4. MetaGameState.getInstance().recordDeath('test') persists to kqgame-meta key
5. SaveManager.createExportData() returns object with both gameState and metaState
</verification>

<success_criteria>
- v1.0 saves load in v2.0 build without errors or data loss
- New saves always include version: 2
- MetaGameState persists independently in kqgame-meta localStorage key
- Export produces valid JSON with both GameState and MetaGameState
- Import validates format and restores both states
- All existing tests continue to pass
- Zero new runtime dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/09-art-pipeline-schema-foundation/09-01-SUMMARY.md`
</output>
